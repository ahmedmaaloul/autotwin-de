# dbt build artefacts. `target/` holds compiled SQL, the manifest and the run results;
# `logs/` holds dbt.log. Both are regenerated by every invocation and neither belongs in git.
target/
dbt_packages/
logs/

# dbt writes an anonymous-usage id here on first run; it is per-machine, not per-project.
.user.yml
